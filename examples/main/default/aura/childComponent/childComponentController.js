({
    handleClick : function(component, event, helper) {
        helper.fireEvents(component, event);
    },
    handleLms : function(component, event, helper) {
        helper.handleLms(component, event);
    }
})
